/**
 * WHOOP dashboard constants.
 *
 * Trimmed for the starter: the production file also carries the WHOOP API
 * endpoints, OAuth scopes and sync/refresh budgets, none of which exist in
 * a demo-data build.
 */

/**
 * The dashboard selects 30 complete days so the Locked In trend can compare
 * a day with its previous week and month.
 */
export const WHOOP_DASHBOARD_HISTORY_DAYS = 30;
