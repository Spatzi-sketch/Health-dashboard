import type { NextConfig } from "next";

/**
 * The starter needs no environment at all: no keys, no release identity,
 * no external hosts. (The production config derives a release identity from
 * git/Vercel here; the starter renders identically without one.)
 */
const nextConfig: NextConfig = {};

export default nextConfig;
